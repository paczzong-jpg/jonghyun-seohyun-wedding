# Snowflake Integration Recipes

Use `sql-api/README.md` when a generated MISO website app needs read-only Snowflake query results through the Snowflake SQL API.

Do not install Snowflake native drivers in the sandbox. Use the included PocketBase route and `runtimeProxy.proxyFetch`.
